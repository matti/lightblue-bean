#!/usr/bin/env sh
set -e

case "$1" in
  "log")
    bean log_serial -n $2
  ;;
  "deploy")
    bean_name=$2
    sketch=$3
    bean program_sketch $sketch -n $bean_name
    
    $0 log $bean_name
  ;;
esac
